# LLM provider configuration
LLM_PROVIDER=openai
LLM_MODEL_FAST=gpt-4o-mini
LLM_MODEL_REASONING=gpt-4.1
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=1500

# Required API key (do NOT commit real keys)
OPENAI_API_KEY=sk-your-key-here

# Local database (DuckDB)
DUCKDB_PATH=data/dk_sentinel.duckdb

# Analyst display name (HITL logs)
ANALYST_NAME=Colby Reichenbach
